# URL of the signaling server. Local dev uses the plain ws:// localhost socket;
# production must be wss:// (an HTTPS page cannot open an insecure ws:// connection).
VITE_SIGNALING_URL=ws://localhost:8787
